// Print numbers 10 to 1 in reverse order through for loop
#include <iostream>
using namespace std;
int main() {
    int num;
    for (int num = 10; num >= 1; num --) {
        cout << num << '\t';
    }
    return 0;
}
